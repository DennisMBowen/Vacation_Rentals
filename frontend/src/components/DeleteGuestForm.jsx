const DeleteGuestForm = ({ rowObject, backendURL, refreshGuest }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeleteGuestForm;